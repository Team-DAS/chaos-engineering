apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-profile
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - profile-cell
  stressors:
    cpu:
      workers: 2
      load: 100
  duration: "60s"